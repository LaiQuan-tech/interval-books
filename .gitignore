# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ─────────────────────────────────────────────────────────────────────────────
# 進銷存合併專用（2026-08）
#
# 這幾條擋的不是雜訊,是外洩。合併作業會把兩個 Lovable 專案的原始碼與一份
# 資料庫備份放進這個目錄底下,而它們含有:
#   - bookstock_260815.backup:auth.users 的 encrypted_password(7 筆)、
#     廠商的身分證字號、統一編號、銀行帳戶
#   - 兩個來源專案的 .env:Supabase anon key
# 這些檔案本身是 untracked,但 untracked 不等於安全 —— 一次 `git add -A`
# 就會全部進版控,而且 git 歷史清不乾淨。
#
# 來源資料夾整個排除是刻意的:程式碼會被「搬移改寫」進 src/ 的正確位置,
# 而不是原樣 commit 進來。搬完之後那兩個資料夾就該從本機刪掉。
# ─────────────────────────────────────────────────────────────────────────────
*.backup
*.dump
.env
**/.env
小時光書店進銷存/
小時光 書店官方網站 v3/
